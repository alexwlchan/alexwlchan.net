---
layout: topic
title: Regular expressions
---
Regular expressions (or "regexes") are sequences of characters used to find matches in text.
For example, `[a-z]+` matches any text made of lowercase characters `a` to `z`, or `[0-9]+\.[0-9]+` matches any decimal number like `42.1`.