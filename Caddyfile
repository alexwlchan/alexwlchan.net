import /home/alexwlchan/repos/finduntaggedtumblrposts.com/Caddyfile

import caddy/alt_alexwlchan_domains.Caddyfile
import caddy/book_redirects.Caddyfile
import caddy/til_redirects.Caddyfile

alexwlchan.net {
	import caddy/caching.Caddyfile
	import caddy/errors.Caddyfile
	import caddy/logging.Caddyfile
	import caddy/security_headers.Caddyfile

	import caddy/redirects.Caddyfile
	import caddy/gone.Caddyfile
	import caddy/projects.Caddyfile

	# Enable compression for responses
	encode zstd gzip

	# Run a static file server for anything not yet handled
	root * /home/alexwlchan/repos/alexwlchan.net/_site
	file_server
}
